
module
{
    interface Elastos.Net.ISocketImpl;

    namespace Elastos{
    namespace Net{

    /**
     * This interface defines a factory for socket implementations.
     */
    interface ISocketImplFactory {
        /**
         * Creates a new {@code SocketImpl} instance.
         *
         * @return the created {@code SocketImpl} instance.
         */
        CreateSocketImpl(
            [out] ISocketImpl** createdInstance);
    }

    } // namespace Net
    } // namespace Elastos

}
