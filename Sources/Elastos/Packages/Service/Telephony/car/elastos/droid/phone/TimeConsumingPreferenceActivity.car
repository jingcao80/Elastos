module
{
    namespace Elastos {
    namespace Droid {
    namespace Telephony {
    namespace Phone {

    interface ITimeConsumingPreferenceListener {
        OnStarted(
            [in] IPreference* preference,
            [in] Boolean reading);
        OnFinished(
            [in] IPreference* preference,
            [in] Boolean reading);
        OnError(
            [in] IPreference* preference,
            [in] Int32 error);
        OnException(
            [in] IPreference* preference,
            [in] ICommandException* exception);
    }

    } // namespace Phone
    } // namespace Telephony
    } // namespace Droid
    } // namespace Elastos
}
