module
{
    namespace Elastos {
    namespace Droid {
    namespace Settings {
    namespace Notification {

    interface ISettingPrefWithCallback {
        OnSettingSelected(
            [in] Int32 value);
    }

    interface ITimePickerPreferenceCallback {
        OnSetTime(
            [in] Int32 hour,
            [in] Int32 minute,
            [out] Boolean* result);
    }

    } // namespace Notification
    } // namespace Settings
    } // namespace Droid
    } // namespace Elastos
}