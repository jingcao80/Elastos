module
{

    namespace Elastos {
    namespace Droid {
    namespace Settings {
    namespace Deviceinfo {

    interface IStorageMeasurement {
    }

    interface IMeasurementDetails {
    }

    interface IStorageMeasurementReceiver {
        UpdateApproximate(
            [in] IStorageMeasurement* meas,
            [in] Int64 totalSize,
            [in] Int64 availSize);

        UpdateDetails(
            [in] IStorageMeasurement* meas,
            [in] IMeasurementDetails* details);
    }

    } // namespace Deviceinfo
    } // namespace Settings
    } // namespace Droid
    } // namespace Elastos
}
