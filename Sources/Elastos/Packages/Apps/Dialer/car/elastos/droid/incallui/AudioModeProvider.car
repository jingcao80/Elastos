
module
{
    namespace Elastos {
    namespace Droid {
    namespace InCallUI {

    interface IAudioModeListener {
        OnAudioMode(
            [in] Int32 newMode);

        OnMute(
            [in] Boolean muted);

        OnSupportedAudioMode(
            [in] Int32 modeMask);
    }

    } // namespace InCallUI
    } // namespace Droid
    } // namespace Elastos
}

