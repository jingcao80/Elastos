
module
{
    namespace Elastos {
    namespace Droid {
    namespace InCallUI {

    interface IContactCacheEntry {
    }

    /**
     * Callback interface for the contact query.
     */
    interface IContactInfoCacheCallback {
        OnContactInfoComplete(
            [in] String callId,
            [in] IContactCacheEntry* entry);

        OnImageLoadComplete(
            [in] String callId,
            [in] IContactCacheEntry* entry);
    }

    } // namespace InCallUI
    } // namespace Droid
    } // namespace Elastos
}
