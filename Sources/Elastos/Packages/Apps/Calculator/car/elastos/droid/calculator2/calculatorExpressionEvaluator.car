module
{
    using interface Elastos.Core.ICharSequence;

    namespace Elastos {
    namespace Droid {
    namespace Calculator2 {

    interface IEvaluateCallback
    {
        OnEvaluate(
            [in] String expr,
            [in] String result,
            [in] Int32 errorResourceId);
    }

    interface ICalculatorExpressionEvaluator
    {
        Evaluate(
            [in] ICharSequence* expr,
            [in] IEvaluateCallback* cbk);

        Evaluate(
            [in] String expr,
            [in] IEvaluateCallback* cbk);
    }

    } // namespace Calculator2
    } // namespace Droid
    } // namespace Elastos

}