
module
{
    interface Elastos.Droid.Content.ISharedPreferences;

    namespace Elastos {
    namespace Droid {
    namespace Inputmethods {
    namespace PinyinIME {

    interface ISettings {
        GetInstance(
            [in] ISharedPreferences* pref);

        WriteBack();

        ReleaseInstance();

        GetKeySound(
            [out] Boolean* keySound);

        SetKeySound(
            [in] Boolean v);

        GetVibrate(
            [out] Boolean* vibrate);

        SetVibrate(
            [in] Boolean v);

        GetPrediction(
            [out] Boolean* prediction);

        SetPrediction(
            [in] Boolean v);
    }

    } // namespace PinyinIME
    } // namespace Inputmethods
    } // namespace Droid
    } // namespace Elastos
}
