<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="Elastos.DevSamples.ContentProviderDemo"
      android:versionCode="1"
      android:versionName="1.0">

    <application android:icon="@null" android:label="@string/app_name">

        <activity android:name="Elastos.DevSamples.ContentProviderDemo.CActivityOne"
                  android:label="@string/app_name"
                  android:theme="@android:style/Theme.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <intent-filter>
                <data android:mimeType="elastos.kesalin.dir" />
            </intent-filter>
            <intent-filter>
                <data android:mimeType="elastos.kesalin.item" />
            </intent-filter>
        </activity>

        <provider android:name="Elastos.DevSamples.ContentProviderDemo.CContentProviderOne"
            android:authorities="elastos.kesalin.contentprovider"
            android:label="@string/app_name"
            android:exported="true">
        </provider>
    </application>

    <!--
    <provider android:authorities="list"
        android:enabled=["true" | "false"]
        android:exported=["true" | "false"]
        android:grantUriPermissions=["true" | "false"]
        android:icon="drawable resource"
        android:initOrder="integer"
        android:label="string resource"
        android:multiprocess=["true" | "false"]
        android:name="string"
        android:permission="string"
        android:process="string"
        android:readPermission="string"
        android:syncable=["true" | "false"]
        android:writePermission="string" >
    </provider>
    -->
</manifest>
