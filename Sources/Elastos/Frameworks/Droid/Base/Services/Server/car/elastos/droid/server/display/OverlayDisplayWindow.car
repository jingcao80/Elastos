module
{
    using interface Elastos.Droid.Graphics.ISurfaceTexture;

    namespace Elastos {
    namespace Droid {
    namespace Server {
    namespace Display {

    [local]
    interface IOverlayDisplayWindowListener {
        OnWindowCreated(
            [in] ISurfaceTexture* surfaceTexture,
            [in] Float refreshRate,
            [in] Int64 presentationDeadlineNanos,
            [in] Int32 state);

        OnWindowDestroyed();

        OnStateChanged(
            [in] Int32 state);
    }

    } // namespace Display
    } // namespace Server
    } // namepsace Droid
    } // namespace Elastos
}
