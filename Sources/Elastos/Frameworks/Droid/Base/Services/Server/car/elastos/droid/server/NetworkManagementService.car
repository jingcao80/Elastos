module
{
    namespace Elastos {
    namespace Droid {
    namespace Server {

    interface INativeDaemonConnectorCallbacks {
        OnDaemonConnected();

        OnEvent(
            [in] Int32 code,
            [in] String raw,
            [in] ArrayOf<String> cooked,
            [out] Boolean* result);
    }

    } // namespace Server
    } // namespace Droid
    } // namespace Elastos

}
