module
{
    namespace Elastos {
    namespace Droid {
    namespace Server {

    interface IDeviceStorageMonitorService {
        const String SERVICE = "devicestoragemonitor";

        UpdateMemory();

        GetMemoryLowThreshold(
            [out] Int64* threshold);

        IsMemoryLow(
            [out] Boolean* threshold);
    }

    }//namespace Server
    }//namespace Droid
    }//namespace Elastos
}