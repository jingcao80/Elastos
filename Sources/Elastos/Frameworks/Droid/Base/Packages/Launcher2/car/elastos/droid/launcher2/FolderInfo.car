module
{
    namespace Elastos {
    namespace Droid {
    namespace Launcher2 {

    interface IFolderListener {
        OnAdd(
            [in] IShortcutInfo* item);

        OnRemove(
            [in] IShortcutInfo* item);

        OnTitleChanged(
            [in] ICharSequence* title);

        OnItemsChanged();
    }

    } // namespace Launcher2
    } // namespace Droid
    } // namespace Elastos
}
