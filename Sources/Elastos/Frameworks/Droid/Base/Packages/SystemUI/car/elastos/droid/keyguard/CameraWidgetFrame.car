
module
{
    using interface Elastos.Droid.Graphics.IRect;

    namespace Elastos {
    namespace Droid {
    namespace SystemUI {
    namespace Keyguard {

    interface ICameraWidgetFrameCallbacks {
        OnLaunchingCamera();

        OnCameraLaunchedSuccessfully();

        OnCameraLaunchedUnsuccessfully();
    }

    interface ICameraWidgetFrame {
        OnScreenTurnedOff();

        SetInsets(
            [in] IRect* insets);

        SetUseFastTransition(
            [in] Boolean useFastTransition);
    }

    } // namespace Keyguard
    } // namespace SystemUI
    } // namespace Droid
    } // namespace Elastos
}