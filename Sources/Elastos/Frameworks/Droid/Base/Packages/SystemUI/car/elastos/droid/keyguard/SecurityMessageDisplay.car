
module
{
    namespace Elastos {
    namespace Droid {
    namespace SystemUI {
    namespace Keyguard {

    interface ISecurityMessageDisplay {
        SetMessage(
            [in] ICharSequence* msg,
            [in] Boolean important);

        SetMessage(
            [in] Int32 resId,
            [in] Boolean important);

        SetMessage(
            [in] Int32 resId,
            [in] Boolean important,
            [in] ArrayOf<IInterface*>* formatArgs);

        SetTimeout(
            [in] Int32 timeout_ms);

        ShowBouncer(
            [in] Int32 animationDuration);

        HideBouncer(
            [in] Int32 animationDuration);
    }

    } // namespace Keyguard
    } // namespace SystemUI
    } // namespace Droid
    } // namespace Elastos
}