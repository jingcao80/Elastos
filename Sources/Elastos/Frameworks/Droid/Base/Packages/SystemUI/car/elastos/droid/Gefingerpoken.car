module
{
    using interface Elastos.Droid.View.IMotionEvent;

    namespace Elastos {
    namespace Droid {
    namespace SystemUI {

    interface IGefingerpoken {
        OnInterceptTouchEvent(
            [in] IMotionEvent* ev,
            [out] Boolean* result);

        OnTouchEvent(
            [in] IMotionEvent* ev,
            [out] Boolean* result);
    }

    } // namespace SystemUI
    } // namespace Droid
    } // namespace Elastos
}
