
module
{
    namespace Elastos {
    namespace Droid {
    namespace SystemUI {
    namespace Keyguard {

    interface IPasswordTextView {
        Append(
            [in] Char32 c);

        DeleteLastChar();

        GetText(
            [out] String* test);

        Reset(
            [in] Boolean animated);
    }

    } // namespace Keyguard
    } // namespace SystemUI
    } // namespace Droid
    } // namespace Elastos
}