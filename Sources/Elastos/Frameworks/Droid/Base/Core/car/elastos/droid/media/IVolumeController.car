
module
{
    namespace Elastos {
    namespace Droid {
    namespace Media {

    interface IIVolumeController {
        displaySafeVolumeWarning(
            [in] Int32 flags);

        volumeChanged(
            [in] Int32 streamType,
            [in] Int32 flags);

        masterVolumeChanged(
            [in] Int32 flags);

        masterMuteChanged(
            [in] Int32 flags);

        setLayoutDirection(
            [in] Int32 layoutDirection);

        dismiss();
    }

    } // namespace Media
    } // namepsace Droid
    } // namespace Elastos
}
