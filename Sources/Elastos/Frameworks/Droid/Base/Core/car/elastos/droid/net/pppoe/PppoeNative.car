
module
{
    namespace Elastos {
    namespace Droid {
    namespace Net {
    namespace Pppoe {

        interface IPppoeNative {

            GetInterfaceName(
                [in] Int32 i,
                [out] String* str);
            GetInterfaceCnt(
                [out] Int32* val);
            InitPppoeNative(
                [out] Int32* val);
            WaitForEvent(
                [out] String* str);
            IsInterfaceAdded(
                [in] String ifname,
                [out] Int32* val);
        }

    } // namespace Pppoe
    } // namespace Net
    } // namespace Droid
    } // namespace Elastos
}
