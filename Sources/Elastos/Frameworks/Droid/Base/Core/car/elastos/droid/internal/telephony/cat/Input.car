
module {

    namespace Elastos {
    namespace Droid {
    namespace Internal {
    namespace Telephony {
    namespace Cat {

    interface IInput {
        DescribeContents(
            [out] Int32* result);

        WriteToParcel(
            [in] Parcel dest, 
            [in] Int32 flags);

        CreateFromParcel(
            [in] Parcel in,
            [out] Input* result);

        NewArray(
            [in] Int32 size);

        SetIcon(
            [in] Bitmap Icon,
            [out] Boolean* result);

    }

    }//Cat
    }//Telephony
    }//Internal
    }//Droid
    }//Elastos

}
