
module
{
    namespace Elastos {
    namespace Droid {
    namespace View {

    interface IApplicationToken {
        WindowsDrawn();

        WindowsVisible();

        WindowsGone();

        KeyDispatchingTimedOut(
            [in] String reason,
            [out] Boolean* res);

        GetKeyDispatchingTimeout(
            [out] Int64* timeOut);
    }

    } // namespace View
    } // namepsace Droid
    } // namespace Elastos
}
