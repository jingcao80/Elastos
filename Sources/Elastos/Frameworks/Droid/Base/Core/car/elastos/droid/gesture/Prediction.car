module
{
    namespace Elastos {
    namespace Droid {
    namespace Gesture {

    interface IPrediction {
        GetName(
            [out] String* name);

        GetScore(
            [out] Double* score);
    }

    } // namespace Gesture
    } // namespace Droid
    } // namespace Elastos
}