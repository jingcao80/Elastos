
module
{

    interface Elastos.Droid.Nfc.ITag;

    namespace Elastos {
    namespace Droid {
    namespace Nfc{
    namespace Tech{

    /**
     * @Involve
     * interace ICloseable
     */
    interface ITagTechnology {
        GetTag(
            [out] ITag** tag);

        Connect();

        Reconnect();

        Close();

        IsConnected(
            [out] Boolean* connected);
    }

    }// namespace Tech
    }// namespace Nfc
    }// namespace Droid
    }// namespace Elastos
}