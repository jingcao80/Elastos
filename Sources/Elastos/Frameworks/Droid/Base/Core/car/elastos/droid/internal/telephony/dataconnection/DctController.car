
module {

    namespace Elastos {
    namespace Droid {
    namespace Internal {
    namespace Telephony {
    namespace DataConnection {

    interface IDctController {
        Boolean IsActiveSubId(Int64 subId);

        IDataStateChangedCallback GetDataStateChangedCallback();

        DctController GetInstance();

        Int32 EnableApnType(Int64 subId, String type);

        Int32 DisableApnType(Int64 subId, String type);

        Boolean IsDataConnectivityPossible(String type, Int32 phoneId);

        Boolean IsIdleOrDeacting(Int32 phoneId);

        SetDefaultDataSubId(Int64 reqSubId);

        DoPsAttach(NetworkRequest n);

        DoPsDetach();

        SetOnDemandDataSubId(NetworkRequest n);

        RegisterForDefaultDataSwitchInfo(Handler h, Int32 what, Object obj);

        RegisterForOnDemandDataSwitchInfo(Handler h, Int32 what, Object obj);

        RegisterForOnDemandPsAttach(Handler h, Int32 what, Object obj);

        RegisterDdsSwitchPropService(Messenger messenger);

        HandleMessage (Message msg);

        DdsSwitchSerializerHandler(Looper looper);

        UnLock();

        Boolean IsLocked();

        HandleMessage (Message msg);

    }

    }//DataConnection
    }//Telephony
    }//Internal
    }//Droid
    }//Elastos

}
