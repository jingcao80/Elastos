
module
{
    namespace Elastos {
    namespace Droid {
    namespace Hardware {
    namespace Hdmi {

    /**
     * Callback interface definition for HDMI client to get informed of
     * the result of various API invocation.
     *
     * @hide
     */
    [oneway]
    interface IIHdmiControlCallback {
        OnComplete(
            [in] Int32 result);
    }

    } // namespace Hdmi
    } // namespace Hardware
    } // namespace Droid
    } // namespace Elastos
}
