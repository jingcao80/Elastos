
module {

    namespace Elastos {
    namespace Droid {
    namespace Internal {
    namespace Telephony {

    interface IIccProvider {
        OnCreate(
            [out] Boolean* result);

        GetType(
            [in] Uri url,
            [out] String* result);

        Insert(
            [in] Uri url, 
            [in] ContentValues initialValues,
            [out] Uri* result);


    }

    }//Telephony
    }//Internal
    }//Droid
    }//Elastos

}
