
module {

    namespace Elastos {
    namespace Droid {
    namespace Internal {
    namespace Telephony {
    namespace DataConnection {

    interface IDdsScheduler {
        DdsScheduler GetInstance();

        Init();

        AddRequest(NetworkRequest req);

        RemoveRequest(NetworkRequest req);

        MarkAccepted(NetworkRequest req);

        Boolean IsAlreadyAccepted(NetworkRequest nr);

        NetworkRequest GetFirstWaitingRequest();

        Boolean AcceptWaitingRequest();

        NotifyRequestAccepted(NetworkRequest nr);

        Boolean IsDdsSwitchRequired(NetworkRequest n);

        Int64 GetCurrentDds();

        UpdateCurrentDds(NetworkRequest n);

        Int64 GetSubIdFromNetworkRequest(NetworkRequest n);

        TriggerSwitch(NetworkRequest n);

        Boolean IsMultiDataSupported();

        Boolean IsAnyRequestWaiting();

    }

    }//DataConnection
    }//Telephony
    }//Internal
    }//Droid
    }//Elastos

}
