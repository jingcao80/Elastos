
module
{
    // interface Elastos.Droid.Internal.Telephony.Cat.IResultCode;

    namespace Elastos {
    namespace Droid {
    namespace Internal {
    namespace Telephony {
    namespace Cat {

    [deprecated]
    interface ICatResponseMessage {
        // SetResultCode(
        //     [in] IResultCode* resCode);

        SetMenuSelection(
            [in] Int32 selection);

        SetInput(
            [in] String input);

        SetEventDownload(
            [in] Int32 event,
            [in] ArrayOf<Byte>* addedInfo);

        SetYesNo(
            [in] Boolean yesNo);

        SetConfirmation(
            [in] Boolean confirm);

        SetAdditionalInfo(
            [in] Int32 info);
    }

    }//Cat
    }//Telephony
    }//Internal
    }//Droid
    }//Elastos

}
