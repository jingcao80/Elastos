
module {

    namespace Elastos {
    namespace Droid {
    namespace Telephony {

    interface IPduCache {
        PduCache GetInstance();

        Boolean Put(Uri uri, PduCacheEntry entry);

        SetUpdating(Uri uri, Boolean updating);

        Boolean IsUpdating(Uri uri);

        PduCacheEntry Purge(Uri uri);

        PurgeAll();

    }

}//Telephony
}//Droid
}//Elastos

}
