
module {

    namespace Elastos {
    namespace Droid {
    namespace Internal {
    namespace Telephony {

    interface IImsSMSDispatcher {
        Dispose();

        HandleMessage(
            [in] Message msg);


        SendRetrySms(
            [in] SmsTracker tracker);

        IsIms(
            [out] Boolean* result);

        GetImsSmsFormat(
            [out] String* result);

        EnableSendSmsOverIms(
            [in] Boolean enable);

        IsImsSmsEnabled(
            [out] Boolean* result);

        ShouldSendSmsOverIms(
            [out] Boolean* result);

    }

    }//Telephony
    }//Internal
    }//Droid
    }//Elastos

}
