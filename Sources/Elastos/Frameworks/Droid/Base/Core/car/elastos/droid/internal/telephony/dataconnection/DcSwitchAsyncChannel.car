
module {

    namespace Elastos {
    namespace Droid {
    namespace Internal {
    namespace Telephony {
    namespace DataConnection {

    interface IDcSwitchAsyncChannel {
        ReqConnect(String type);

        Int32 RspConnect(Message response);

        Int32 ConnectSync(String type);

        ReqDisconnect(String type);

        Int32 RspDisconnect(Message response);

        Int32 DisconnectSync(String type);

        ReqIsIdle();

        Boolean RspIsIdle(Message response);

        Boolean IsIdleSync();

        ReqIsIdleOrDeacting();

        Boolean RspIsIdleOrDeacting(Message response);

        Boolean IsIdleOrDeactingSync();

    }

    }//DataConnection
    }//Telephony
    }//Internal
    }//Droid
    }//Elastos

}
