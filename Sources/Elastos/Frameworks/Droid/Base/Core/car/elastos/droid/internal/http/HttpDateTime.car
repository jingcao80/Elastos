module
{
    namespace Elastos {
    namespace Droid {
    namespace Internal {
    namespace Http {

    /**
     * Helper for parsing an HTTP date.
     */
    [local]
    interface IHttpDateTime {
        Parse(
            [in] String timeString,
            [out] Int64* time);
    }

    } // namespace Http
    } // namespace Internal
    } // namespace Droid
    } // namespace Elastos
}
