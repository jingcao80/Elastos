module
{
    interface Elastos.Droid.Os.IIBatteryPropertiesListener;

    namespace Elastos {
    namespace Droid {
    namespace Os {

    interface IIBatteryPropertiesRegistrar {

        RegisterListener(
            [in] IIBatteryPropertiesListener* listener);

        UnregisterListener(
            [in] IIBatteryPropertiesListener* listener);

        GetProperty(
            [in] Int32 id,
            [out] IBatteryProperties** prop);
    }

    } // namespace Os
    } // namespace Droid
    } // namespace Elastos
}
