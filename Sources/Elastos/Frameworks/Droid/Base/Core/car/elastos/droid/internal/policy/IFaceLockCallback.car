
module
{
    namespace Elastos {
    namespace Droid {
    namespace Internal {
    namespace Policy {

    /** {@hide} */
    [oneway]
    interface IIFaceLockCallback {
        Unlock();

        Cancel();

        ReportFailedAttempt();

        PokeWakelock(
            [in] Int32 millis);
    }

    } // Policy
    } // Internal
    } // Droid
    } // Elastos
}
