module
{
    namespace Elastos {
    namespace Droid {
    namespace Media {
    namespace Projection {

    interface IIMediaProjectionWatcherCallback {
        OnStart(
            [in] IMediaProjectionInfo * info);

        OnStop(
            [in] IMediaProjectionInfo * info);
    }

    } // namespace Projection
    } // namespace media
    } // namepsace Droid
    } // namespace Elastos
}
