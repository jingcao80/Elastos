
module
{
    namespace Elastos {
    namespace Droid {
    namespace Os {

    /** {@hide} */
    interface IIHardwareService {
        // obsolete flashlight support
        GetFlashlightEnabled(
            [out] Boolean* enabled);

        SetFlashlightEnabled(
            [in] Boolean on);
    }

    } // namespace Os
    } // namespace Droid
    } // namespace Elastos
}
