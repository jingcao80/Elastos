
module {

    namespace Elastos {
    namespace Droid {
    namespace Internal {
    namespace Telephony {

    interface ITelephonyDevController {
        TelephonyDevController Create();

        TelephonyDevController GetInstance();

        RegisterRIL(CommandsInterface cmdsIf);

        UnregisterRIL(CommandsInterface cmdsIf);

        HandleMessage(Message msg);

        Int32 GetModemCount();

        HardwareConfig GetModem(Int32 index);

        Int32 GetSimCount();

        HardwareConfig GetSim(Int32 index);

        HardwareConfig GetModemForSim(Int32 simIndex);

        GetAllSimsForModem(Int32 modemIndex);

        GetAllModems();

        GetAllSims();

    }

    }//Telephony
    }//Internal
    }//Droid
    }//Elastos

}
