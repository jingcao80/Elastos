module
{
    namespace Elastos {
    namespace Droid {
    namespace App {

    /**
     * Callback to find out when we have finished stopping a user.
     * {@hide}
     */
    interface IStopUserCallback {
        UserStopped(
            [in] Int32 userId);

        UserStopAborted(
            [in] Int32 userId);
    }

    } // namespace App
    } // namespace Droid
    } // namespace Elastos
}
