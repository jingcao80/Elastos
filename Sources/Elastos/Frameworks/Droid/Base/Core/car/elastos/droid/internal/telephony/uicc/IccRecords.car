
module {

    namespace Elastos {
    namespace Droid {
    namespace Internal {
    namespace Telephony {
    namespace Uicc {

    interface IIccRecords {
        Dispose();

        RecordsRequired();

        AdnRecordCache GetAdnCache();

        String GetIccId();

        RegisterForRecordsLoaded(Handler h, Int32 what, Object obj);

        UnregisterForRecordsLoaded(Handler h);

        RegisterForImsiReady(Handler h, Int32 what, Object obj);

        UnregisterForImsiReady(Handler h);

        RegisterForRecordsEvents(Handler h, Int32 what, Object obj);

        UnregisterForRecordsEvents(Handler h);

        RegisterForNewSms(Handler h, Int32 what, Object obj);

        UnregisterForNewSms(Handler h);

        String GetIMSI();

        SetImsi(String imsi);

        String GetMsisdnNumber();

        String GetGid1();

        String GetMsisdnAlphaTag();

        String GetVoiceMailNumber();

        String GetServiceProviderName();

        String GetVoiceMailAlphaTag();

        Boolean GetRecordsLoaded();

        HandleMessage(Message msg);

        Boolean IsCspPlmnEnabled();

        String GetOperatorNumeric();

        Boolean IsCallForwardStatusStored();

        Boolean GetVoiceCallForwardingFlag();

        SetVoiceCallForwardingFlag(Int32 line, Boolean enable, String number);

        IsProvisioned ();

        IsimRecords GetIsimRecords();

        UsimServiceTable GetUsimServiceTable();

        String GetIccSimChallengeResponse(Int32 authContext, String data);

        Int32 GetSmsCapacityOnIcc();

    }

    }//Uicc
    }//Telephony
    }//Internal
    }//Droid
    }//Elastos

}
