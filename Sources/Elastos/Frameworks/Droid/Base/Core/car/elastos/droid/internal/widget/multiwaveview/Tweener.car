
module
{
    namespace Elastos {
    namespace Droid {
    namespace Internal {
    namespace Widget {
    namespace Multiwaveview {

    [deprecated]
    interface ITweener {

        From(
            [in] IInterface* object,
            [in] Int64 duration,
            [in] ArrayOf<IInterface*>* vars,
            [out] ITweener** result);
    }

    [deprecated]
    interface ITweenerHelper {

        To(
            [in] IInterface* object,
            [in] Int64 duration,
            [in] ArrayOf<IInterface*>* vars,
            [out] ITweener** result);

        Reset();
    }

    } // namespace Widget
    } // namespace Widget
    } // namespace Internal
    } // namespace Droid
    } // namespace Elastos
}
