
module
{
    namespace Elastos {
    namespace Droid {
    namespace Accounts {

    interface IAccountAndUser {
        Equals(
            [in] IInterface *o,
            [out] Boolean *isEqual);

        GetHashCode(
            [out] Int32 *hash);

        ToString(
            [out] String *info);

        GetAccount(
            [out] IAccount **account);

        GetUserId(
            [out] Int32 *id);
    }

    } // Accounts
    } // Droid
    } // Elastos
}
