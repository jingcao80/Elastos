
module {

    namespace Elastos {
    namespace Droid {
    namespace Internal {
    namespace Telephony {

    interface IServiceStateTracker {
        RequestShutdown();

        Dispose();

        Boolean GetDesiredPowerState();


        RegisterForDataConnectionAttached(Handler h, Int32 what, Object obj);

        UnregisterForDataConnectionAttached(Handler h);

        RegisterForDataConnectionDetached(Handler h, Int32 what, Object obj);

        UnregisterForDataConnectionDetached(Handler h);

        RegisterForDataRegStateOrRatChanged(Handler h, Int32 what, Object obj);

        UnregisterForDataRegStateOrRatChanged(Handler h);

        RegisterForNetworkAttached(Handler h, Int32 what, Object obj);

        UnregisterForNetworkAttached(Handler h);

        RegisterForPsRestrictedEnabled(Handler h, Int32 what, Object obj);

        UnregisterForPsRestrictedEnabled(Handler h);

        RegisterForPsRestrictedDisabled(Handler h, Int32 what, Object obj);

        UnregisterForPsRestrictedDisabled(Handler h);

        PowerOffRadioSafely(DcTrackerBase dcTracker);

        Boolean ProcessPendingRadioPowerOffAfterDataOff();

        String GetSystemProperty(String property, String defValue);

        GetAllCellInfo();

        SignalStrength GetSignalStrength();

    }

    }//Telephony
    }//Internal
    }//Droid
    }//Elastos

}
