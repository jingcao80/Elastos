
module
{
    interface Elastos.Droid.View.IView;

    namespace Elastos {
    namespace Droid {
    namespace Accounts {

    /**
     * @hide
     * Just shows an error message about the account restrictions for the limited user.
     */
    [local]
    interface ICantAddAccountActivity {

        const String EXTRA_ERROR_CODE = "android.accounts.extra.ERROR_CODE";

        OnCancelButtonClicked(
            [in] IView* view);
    }

    } // Accounts
    } // Droid
    } // Elastos
}
