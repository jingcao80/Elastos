module
{
    interface Elastos.Droid.Content.IContext;

    namespace Elastos {
    namespace Droid {
    namespace Internal {
    namespace Utility {
    namespace Cm {

    interface IScreenType {
        IsPhone(
            [in] IContext* ctx,
            [out] Boolean* result);

        IsHybrid(
            [in] IContext* ctx,
            [out] Boolean* result);

        IsTablet(
            [in] IContext* ctx,
            [out] Boolean* result);
    }

    }//namespace Cm
    }//namespace Utility
    }//namespace Internal
    }//namespace Droid
    }//namespace Elastos
}
