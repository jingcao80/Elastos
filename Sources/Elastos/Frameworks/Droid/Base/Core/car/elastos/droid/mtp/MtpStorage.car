
module
{
    namespace Elastos {
    namespace Droid {
    namespace Mtp {

    interface IMtpStorage {
        GetStorageId(
            [out] Int32* result);

        GetPath(
            [out] String* result);

        GetDescription(
            [out] String* result);

        GetReserveSpace(
            [out] Int64* result);

        IsRemovable(
            [out] Boolean* result);

        GetMaxFileSize(
            [out] Int64* result);
    }

    interface IMtpStorageHelper {
        GetStorageId(
            [in] Int32 index,
            [out] Int32* result);
    }

    } // namespace mtp
    } // namepsace Droid
    } // namespace Elastos
}