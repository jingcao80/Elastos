
module {

    namespace Elastos {
    namespace Droid {
    namespace Internal {
    namespace Telephony {
    namespace DataConnection {

    interface IDcTrackerBase {
        Run();

        interface IDcTrackerBaseTxRxSum {
            Reset();

            UpdateTxRxSum();

        }

        Dispose();

        Activity GetActivity();

        Boolean IsApnTypeActive(String type);

        GetActiveApnTypes();

        String GetActiveApnString(String apnType);

        SetDataOnRoamingEnabled(Boolean enabled);

        Boolean GetDataOnRoamingEnabled();

        SetDataEnabled(Boolean enable);

        Boolean GetDataEnabled();

        HandleMessage(Message msg);

        Boolean GetAnyDataEnabled();

        LinkProperties GetLinkProperties(String apnType);

        NetworkCapabilities GetNetworkCapabilities(String apnType);

        Boolean IsApnTypeEnabled(String apnType);

        Boolean SetInternalDataEnabled(Boolean enable);

        CleanUpAllConnections(String cause);

        StartNetStatPoll();

        StopNetStatPoll();

        UpdateDataActivity();

        Int32 GetRecoveryAction();

        PutRecoveryAction(Int32 action);

    }

    }//DataConnection
    }//Telephony
    }//Internal
    }//Droid
    }//Elastos

}
