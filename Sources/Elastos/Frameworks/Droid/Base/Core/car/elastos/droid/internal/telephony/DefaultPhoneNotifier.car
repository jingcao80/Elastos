
module {

    namespace Elastos {
    namespace Droid {
    namespace Internal {
    namespace Telephony {

    interface IDefaultPhoneNotifier {
        NotifyPhoneState(Phone sender);

        NotifyServiceState(Phone sender);

        NotifySignalStrength(Phone sender);

        NotifyMessageWaitingChanged(Phone sender);

        NotifyCallForwardingChanged(Phone sender);

        NotifyDataActivity(Phone sender);

        NotifyDataConnectionFailed(Phone sender, String reason, String apnType);

        NotifyCellLocation(Phone sender);

        Catch (RemoteException ex);

        Catch (RemoteException ex);

        NotifyDisconnectCause(Int32 cause, Int32 preciseCause);

        Catch (RemoteException ex);

        NotifyVoLteServiceStateChanged(Phone sender, VoLteServiceState lteState);

        State ConvertCallState(Int32 state);

        DataState ConvertDataState(Int32 state);

        DataActivityState ConvertDataActivityState(Int32 state);

        State ConvertPreciseCallState(Int32 state);

    }

    }//Telephony
    }//Internal
    }//Droid
    }//Elastos

}
