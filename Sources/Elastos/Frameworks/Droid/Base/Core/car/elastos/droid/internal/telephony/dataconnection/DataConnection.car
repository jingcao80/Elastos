
module {

    namespace Elastos {
    namespace Droid {
    namespace Internal {
    namespace Telephony {
    namespace DataConnection {

    interface IDataConnection {
        String CmdToString(Int32 cmd);

        Dispose();

        NetworkCapabilities GetCopyNetworkCapabilities();

        LinkProperties GetCopyLinkProperties();

        Boolean GetIsInactive();

        Int32 GetCid();

        ApnSetting GetApnSetting();

        SetLinkPropertiesHttpProxy(ProxyInfo proxy);

        Boolean IsIpv4Connected();

        Boolean IsIpv6Connected();

        UpdateLinkPropertyResult UpdateLinkProperty(DataCallResponse newState);

        Int32 GetDataConnectionId();

        Exit();

        Boolean ProcessMessage(Message msg);

        Slog(String s);

        String ToStringSimple();

    }

    }//DataConnection
    }//Telephony
    }//Internal
    }//Droid
    }//Elastos

}
