
module
{
    namespace Elastos {
    namespace Droid {
    namespace Media {

    interface IIRemoteDisplayProvider {
        SetCallback(
            [in] IIRemoteDisplayCallback * cb);

        SetDiscoveryMode(
            [in] Int32 mode);

        Connect(
            [in] String id);

        Disconnect(
            [in] String id);

        SetVolume(
            [in] String id,
            [in] Int32 volume);

        AdjustVolume(
            [in] String id,
            [in] Int32 delta);

    }
    } // namespace media
    } // namepsace Droid
    } // namespace Elastos
}
