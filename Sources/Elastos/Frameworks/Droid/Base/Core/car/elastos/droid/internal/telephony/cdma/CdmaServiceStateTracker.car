
module {

    namespace Elastos {
    namespace Droid {
    namespace Internal {
    namespace Telephony {
    namespace Cdma {

    interface ICdmaServiceStateTracker {
        OnChange(Boolean selfChange);

        OnChange(Boolean selfChange);

        Dispose();

        RegisterForSubscriptionInfoReady(Handler h, Int32 what, Object obj);

        UnregisterForSubscriptionInfoReady(Handler h);

        HandleMessage (Message msg);

    }

    }//Cdma
    }//Telephony
    }//Internal
    }//Droid
    }//Elastos

}
