
module {

    namespace Elastos {
    namespace Droid {
    namespace Internal {
    namespace Telephony {

    interface ISubscription {
        Equals(
            [in] Subscription sub,
            [out] Boolean* result);

        IsSame(
            [in] Subscription sub,
            [out] Boolean* result);

        Clear();

        CopyFrom(
            [in] Subscription from,
            [out] Subscription* result);

        GetAppIndex(
            [out] Int32* result);

    }

    }//Telephony
    }//Internal
    }//Droid
    }//Elastos

}
