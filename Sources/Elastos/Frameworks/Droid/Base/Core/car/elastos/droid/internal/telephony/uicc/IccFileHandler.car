
module {

    namespace Elastos {
    namespace Droid {
    namespace Internal {
    namespace Telephony {
    namespace Uicc {

    interface IIccFileHandler {
        Dispose();

        LoadEFLinearFixed(Int32 fileid, String path, Int32 recordNum, Message onLoaded);

        LoadEFLinearFixed(Int32 fileid, Int32 recordNum, Message onLoaded);

        LoadEFImgLinearFixed(Int32 recordNum, Message onLoaded);

        GetEFLinearRecordSize(Int32 fileid, String path, Message onLoaded);

        GetEFLinearRecordSize(Int32 fileid, Message onLoaded);

        LoadEFLinearFixedAll(Int32 fileid, Message onLoaded);

        LoadEFLinearFixedAll(Int32 fileid, String path, Message onLoaded);

        LoadEFTransparent(Int32 fileid, Message onLoaded);

        LoadEFTransparent(Int32 fileid, Int32 size, Message onLoaded);

        HandleMessage(Message msg);
    }

    }//Uicc
    }//Telephony
    }//Internal
    }//Droid
    }//Elastos

}
