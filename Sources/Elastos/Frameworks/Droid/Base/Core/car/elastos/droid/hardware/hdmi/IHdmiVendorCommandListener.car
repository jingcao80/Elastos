
module
{
    namespace Elastos {
    namespace Droid {
    namespace Hardware {
    namespace Hdmi {

    /**
     * Callback interface definition for HDMI client to get the vendor-specific
     * commands.
     *
     * @hide
     */
    [oneway, deprecated]
    interface IIHdmiVendorCommandListener {
        OnReceived(
            [in] Int32 logicalAddress,
            [in] ArrayOf<Byte>* operands,
            [in] Boolean hasVendorId);
    }

    } // namespace Hdmi
    } // namespace Hardware
    } // namespace Droid
    } // namespace Elastos
}
