
module
{
    interface Elastos.Droid.Os.IHandler;

    namespace Elastos {
    namespace Droid {
    namespace Internal {
    namespace Telephony {
    namespace DataConnection {

    /**
     * @Involve
     * interface IStateMachine
     */
    interface IDcSwitchState {
        NotifyDataConnection(
            [in] Int32 phoneId,
            [in] String state,
            [in] String reason,
            [in] String apnName,
            [in] String apnType,
            [in] Boolean unavailable);

        CleanupAllConnection();

        RegisterForIdle(
            [in] IHandler* h,
            [in] Int32 what,
            [in] IInterface* obj);

        UnregisterForIdle(
            [in] IHandler* h);

        TransitToIdleState();

        TransitToActingState();
    }

    }//DataConnection
    }//Telephony
    }//Internal
    }//Droid
    }//Elastos

}
