
module {

    namespace Elastos {
    namespace Droid {
    namespace Internal {
    namespace Telephony {
    namespace Cdma {

    interface ICdmaCallTracker {
        Dispose();

        RegisterForVoiceCallStarted(Handler h, Int32 what, Object obj);

        UnregisterForVoiceCallStarted(Handler h);

        RegisterForVoiceCallEnded(Handler h, Int32 what, Object obj);

        UnregisterForVoiceCallEnded(Handler h);

        RegisterForCallWaiting(Handler h, Int32 what, Object obj);

        UnregisterForCallWaiting(Handler h);


        Boolean IsInEmergencyCall();

    }

    }//Cdma
    }//Telephony
    }//Internal
    }//Droid
    }//Elastos

}
