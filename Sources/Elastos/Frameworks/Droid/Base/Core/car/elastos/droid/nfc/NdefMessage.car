
module
{

    namespace Elastos {
    namespace Droid {
    namespace Nfc{

    /**
     * @Involve
     * interace IParcelable
     */
    interface INdefMessage {
        GetRecords(
            [out, callee] ArrayOf<INdefRecord*>* res);

        GetByteArrayLength(
            [out] Int32* len);

        ToByteArray(
            [out, callee] ArrayOf<Byte>* ba);

    }

    }// namespace Nfc
    }// namespace Droid
    }// namespace Elastos
}