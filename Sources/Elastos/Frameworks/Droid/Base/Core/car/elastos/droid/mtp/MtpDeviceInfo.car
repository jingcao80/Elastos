
module
{
    namespace Elastos {
    namespace Droid {
    namespace Mtp {

    interface IMtpDeviceInfo {
        GetManufacturer(
            [out] String* result);

        SetManufacturer(
            [in] String str);

        GetModel(
            [out] String* result);

        SetModel(
            [in] String str);

        GetVersion(
            [out] String* result);

        SetVersion(
            [in] String str);

        GetSerialNumber(
            [out] String* result);

        SetSerialNumber(
            [in] String str);
    }
    } // namespace mtp
    } // namepsace Droid
    } // namespace Elastos
}