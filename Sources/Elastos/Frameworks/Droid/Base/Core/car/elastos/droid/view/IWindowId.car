
module
{
    namespace Elastos {
    namespace Droid {
    namespace View {

    /** {@hide} */
    interface IIWindowId {
        RegisterFocusObserver(
            [in] IIWindowFocusObserver* observer);

        UnregisterFocusObserver(
            [in] IIWindowFocusObserver* observer);

        IsFocused(
            [out] Boolean* result);
    }

    } // namespace View
    } // namepsace Droid
    } // namespace Elastos
}
