
module {

    namespace Elastos {
    namespace Droid {
    namespace Internal {
    namespace Telephony {

    interface IInboundSmsHandler {
        Dispose();

        UpdatePhoneObject(PhoneBase phone);

        PhoneBase GetPhone();

        Boolean ProcessMessage(Message msg);

        Boolean ProcessMessage(Message msg);

        Enter();

        Exit();

        Boolean ProcessMessage(Message msg);

        Enter();

        Exit();

        Boolean ProcessMessage(Message msg);

        Boolean ProcessMessage(Message msg);

        HandleNewSms(AsyncResult ar);

        HandleInjectSms(AsyncResult ar);

        Int32 DispatchMessage(SmsMessageBase smsb);

        Boolean ProcessMessagePart(InboundSmsTracker tracker);

        SetAndDirectIntent(Intent intent, Int32 destPort);

        Boolean IsCurrentFormat3gpp2();

    }

    }//Telephony
    }//Internal
    }//Droid
    }//Elastos

}
