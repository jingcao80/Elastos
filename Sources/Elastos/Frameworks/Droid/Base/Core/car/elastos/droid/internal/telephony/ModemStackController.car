
module {

    namespace Elastos {
    namespace Droid {
    namespace Internal {
    namespace Telephony {

    interface IModemStackController {
        interface IModemStackControllerModemCapabilityInfo {
        }

        Int32 GetStackId();

        OnReceive(Context context, Intent intent);

        ModemStackController GetInstance();

        HandleMessage(Message msg);

        Boolean IsStackReady();

        Int32 GetMaxDataAllowed();

        Int32 GetCurrentStackIdForPhoneId(Int32 phoneId);

        Int32 GetPrimarySub();

        ModemCapabilityInfo GetModemRatCapsForPhoneId(Int32 phoneId);

        RegisterForStackReady(Handler h, Int32 what, Object obj);

        RegisterForModemRatCapsAvailable(Handler h, Int32 what, Object obj);

        RegisterForModemDataCapsUpdate(Handler h, Int32 what, Object obj);

    }

    }//Telephony
    }//Internal
    }//Droid
    }//Elastos

}
