
module {

    namespace Elastos {
    namespace Droid {
    namespace Internal {
    namespace Telephony {
    namespace Cat {

    interface ICatLog {
        D(
            [in] IInterface* caller,
            [in] String msg);

        D(
            [in] String caller,
            [in] String msg);

        E(
            [in] IInterface* caller,
            [in] String msg);

        E(
            [in] String caller,
            [in] String msg);

    }

    }//Cat
    }//Telephony
    }//Internal
    }//Droid
    }//Elastos

}
