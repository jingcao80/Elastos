// Copyright 2014 The Chromium Authors. All rights reserved.
// Use of this source code is governed by a BSD-style license that can be
// found in the LICENSE file.

// This file has been auto-generated by code_generator_v8.py. DO NOT MODIFY!

#include "config.h"
#include "V8HeaderMapForEachCallback.h"

#include "bindings/modules/v8/V8HeaderMap.h"
#include "bindings/v8/V8Binding.h"
#include "bindings/v8/V8Callback.h"
#include "core/dom/ExecutionContext.h"
#include "wtf/Assertions.h"
#include "wtf/GetPtr.h"
#include "wtf/RefPtr.h"

namespace WebCore {

V8HeaderMapForEachCallback::V8HeaderMapForEachCallback(v8::Handle<v8::Function> callback, ScriptState* scriptState)
    : ActiveDOMCallback(scriptState->executionContext())
    , m_scriptState(scriptState)
{
    m_callback.set(scriptState->isolate(), callback);
}

V8HeaderMapForEachCallback::~V8HeaderMapForEachCallback()
{
}

bool V8HeaderMapForEachCallback::handleItem(ScriptValue thisValue, const String& value, const String& key, HeaderMap* map)
{
    if (!canInvokeCallback())
        return true;

    v8::Isolate* isolate = m_scriptState->isolate();
    if (m_scriptState->contextIsEmpty())
        return true;

    ScriptState::Scope scope(m_scriptState.get());
    v8::Handle<v8::Value> thisHandle = thisValue.v8Value();
    if (thisHandle.IsEmpty()) {
        if (!isScriptControllerTerminating())
            CRASH();
        return true;
    }
    v8::Handle<v8::Value> valueHandle = v8String(isolate, value);
    if (valueHandle.IsEmpty()) {
        if (!isScriptControllerTerminating())
            CRASH();
        return true;
    }
    v8::Handle<v8::Value> keyHandle = v8String(isolate, key);
    if (keyHandle.IsEmpty()) {
        if (!isScriptControllerTerminating())
            CRASH();
        return true;
    }
    v8::Handle<v8::Value> mapHandle = toV8(map, m_scriptState->context()->Global(), isolate);
    if (mapHandle.IsEmpty()) {
        if (!isScriptControllerTerminating())
            CRASH();
        return true;
    }
    v8::Handle<v8::Value> argv[] = { valueHandle, keyHandle, mapHandle };

    return invokeCallback(m_scriptState.get(), m_callback.newLocal(isolate), thisHandle, 3, argv);
}

bool V8HeaderMapForEachCallback::handleItem(const String& value, const String& key, HeaderMap* map)
{
    if (!canInvokeCallback())
        return true;

    v8::Isolate* isolate = m_scriptState->isolate();
    if (m_scriptState->contextIsEmpty())
        return true;

    ScriptState::Scope scope(m_scriptState.get());
    v8::Handle<v8::Value> valueHandle = v8String(isolate, value);
    if (valueHandle.IsEmpty()) {
        if (!isScriptControllerTerminating())
            CRASH();
        return true;
    }
    v8::Handle<v8::Value> keyHandle = v8String(isolate, key);
    if (keyHandle.IsEmpty()) {
        if (!isScriptControllerTerminating())
            CRASH();
        return true;
    }
    v8::Handle<v8::Value> mapHandle = toV8(map, m_scriptState->context()->Global(), isolate);
    if (mapHandle.IsEmpty()) {
        if (!isScriptControllerTerminating())
            CRASH();
        return true;
    }
    v8::Handle<v8::Value> argv[] = { valueHandle, keyHandle, mapHandle };

    return invokeCallback(m_scriptState.get(), m_callback.newLocal(isolate), 3, argv);
}

} // namespace WebCore
