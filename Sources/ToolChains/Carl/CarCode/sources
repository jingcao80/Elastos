
TARGET_NAME= carcode
TARGET_TYPE = exe

SOURCES = carcode.cpp

ELASTOS_LIBS = ../ElCar/Carc/clsbase/clsbase.lib
ELASTOS_LIBS += ../ElCar/Carc/clsutil/clsutil.lib
ifeq "$(XDK_TARGET_PLATFORM)" "linux"
LIBRARIES += -lz
else
LIBRARIES += $(XDK_USER_LIB)/libz.lib
endif
LIBRARIES += -lstdc++
